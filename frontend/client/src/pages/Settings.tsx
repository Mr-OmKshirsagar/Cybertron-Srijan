import { useState } from "react";
import { Bell, Check, ChevronRight, Clock3, LockKeyhole, Mail, Mic, Moon, ShieldCheck, SlidersHorizontal, Volume2 } from "lucide-react";

function Toggle({ value, onChange }: { value: boolean; onChange: () => void }) { return <button className={`settings-toggle ${value ? "on" : ""}`} onClick={onChange} aria-pressed={value}><span /></button>; }

export default function Settings() {
  const [voice, setVoice] = useState(true); const [email, setEmail] = useState(true); const [weekly, setWeekly] = useState(false); const [compact, setCompact] = useState(false);
  return (
    <div className="page-content page-enter settings-page">
      <div className="page-header"><div><div className="eyebrow"><span className="eyebrow-line" /> WORKSPACE PREFERENCES</div><h1>Make LegalLens <span className="lime-dot">yours.</span></h1><p>Control how your workspace reads, speaks, and remembers.</p></div><div className="saved-state"><Check size={13} /> All changes saved</div></div>
      <div className="settings-layout"><aside className="settings-nav panel"><button className="active"><SlidersHorizontal size={15} /> Preferences</button><button><Bell size={15} /> Notifications <span>3</span></button><button><LockKeyhole size={15} /> Privacy & data</button><button><Mic size={15} /> Voice assistant</button><button><ShieldCheck size={15} /> Security</button></aside><section className="settings-sections"><div className="panel setting-card"><div className="setting-title"><div className="setting-icon lime-bg"><SlidersHorizontal size={16} /></div><div><h2>Workspace preferences</h2><p>Shape the density and tone of your intelligence workspace.</p></div></div><div className="setting-row"><div><strong>Compact dashboard</strong><span>Fit more clauses and metrics above the fold.</span></div><Toggle value={compact} onChange={() => setCompact(!compact)} /></div><div className="setting-row"><div><strong>Dark mode</strong><span>Keep the high-contrast LegalLens canvas active.</span></div><div className="setting-value"><Moon size={14} /> Always on</div></div><div className="setting-row"><div><strong>Timezone</strong><span>Used for deadlines and scheduled reminders.</span></div><button className="select-value">Asia/Kolkata <ChevronRight size={14} /></button></div></div><div className="panel setting-card"><div className="setting-title"><div className="setting-icon amber-bg"><Volume2 size={16} /></div><div><h2>Voice assistant</h2><p>Make grounded answers easier to consume hands-free.</p></div></div><div className="setting-row"><div><strong>Voice playback</strong><span>Show the Hear answer action in Copilot.</span></div><Toggle value={voice} onChange={() => setVoice(!voice)} /></div><div className="setting-row"><div><strong>Voice style</strong><span>Optimized for clear clause citations.</span></div><button className="select-value">Calm · Indian English <ChevronRight size={14} /></button></div><div className="voice-preview"><button><PlayIcon /></button><div><strong>“According to Clause 12 on page 5…”</strong><span>Preview citation pronunciation</span></div><div className="waveform">{Array.from({ length: 18 }).map((_, index) => <i key={index} style={{ height: `${8 + ((index * 13) % 18)}px` }} />)}</div></div></div><div className="panel setting-card"><div className="setting-title"><div className="setting-icon coral-bg"><Mail size={16} /></div><div><h2>Notifications</h2><p>Choose where LegalLens meets you outside the workspace.</p></div></div><div className="setting-row"><div><strong>Deadline emails</strong><span>Receive T−72h, T−24h, and T−5h reminders.</span></div><Toggle value={email} onChange={() => setEmail(!email)} /></div><div className="setting-row"><div><strong>Weekly contract digest</strong><span>A short review of open risks and upcoming obligations.</span></div><Toggle value={weekly} onChange={() => setWeekly(!weekly)} /></div><div className="email-card settings-email"><div className="email-icon"><Mail size={16} /></div><div><strong>om.mehta@example.com</strong><span>Primary notification address</span></div><span className="verified-label"><Check size={12} /> Verified</span></div></div><div className="panel privacy-card"><div className="privacy-card-icon"><ShieldCheck size={18} /></div><div><h2>Your privacy promise</h2><p>Raw documents are processed in memory only. Structured intelligence expires with this session after 24 hours. No source file is written to disk or blob storage.</p><button className="text-link">Read the full privacy architecture <ChevronRight size={14} /></button></div><Clock3 size={16} className="privacy-clock" /></div></section></div>
    </div>
  );
}

function PlayIcon() { return <span className="play-triangle" />; }
